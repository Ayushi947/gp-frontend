/**
 * Generated by orval v7.18.0 🍺
 * Do not edit manually.
 * GlidingPath 401(k) Administration API
 * Comprehensive REST API for retirement plan administration, recordkeeping, and participant management. Provides endpoints for plan sponsors, participants, administrators, and integrations.
 * OpenAPI spec version: 1.0.0
 */

/**
 * Rule configuration object with required DRL content and metadata
 */
export interface RuleConfigDTO {
  /** Unique identifier for the rule (assigned by system) */
  readonly id?: string;
  /** Unique name identifier for the rule (e.g., 'eligibility', 'contribution-calculation'). Should be lowercase with hyphens. Used for lookups and Business Central integration. */
  name: string;
  /** DRL (Drools Rule Language) rule content. Must contain valid DRL syntax with package, rule definition, when/then clauses. Example: package rules; rule \"MyRule\" when Participant(age >= 21) then ... end */
  ruleContent: string;
  /** Timestamp of last modification (assigned by system, read-only) */
  readonly lastUpdated?: string;
  /** Human-readable description of the rule's purpose and functionality. Optional field for documentation. */
  description?: string;
  /** Active status indicating if the rule is currently used by the Drools engine. Inactive rules are stored but not executed during rule evaluation. */
  active?: boolean;
  /** Semantic version of the rule for tracking changes (e.g., '1.0', '1.2.3'). Helps with rule evolution and compatibility tracking. */
  version?: string;
}
